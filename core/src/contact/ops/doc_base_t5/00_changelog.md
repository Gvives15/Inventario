Changelog — Doc Base T5

- v0.2 (T5):
  - Added `ZoneModel` and `Contact.zone` support.
  - Implemented separated `ReviewQueueAdmin` (Proxy Model).
  - Cleaned up `OrderAdmin` (removed deadline noise).
  - Added `ZoneFilter` and bulk assign actions.

- v0.1 (Inicial): estructura modular creada, índice y plantillas base.

